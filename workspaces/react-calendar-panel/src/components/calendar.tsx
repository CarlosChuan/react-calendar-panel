import React from "react";


export const Calendar = () => {
  // const [selectedDay, setSelectedDay] = useState<Day<EventType>>(new Day(new Date(), [], true));

  return ( 
    <div>
      This will be the calendar
    </div>
  )
}